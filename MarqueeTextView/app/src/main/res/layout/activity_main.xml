<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:shinx="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:orientation="vertical"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <widgets.MarqueeTextView
        android:id="@+id/marquee"
        android:layout_width="300dp"
        android:textSize="30sp"
        android:layout_height="wrap_content"
        android:text="@string/marquee"
        android:textColor="#000000"
        android:layout_gravity="center_horizontal"
        android:singleLine="true"/>

    <Button
        android:id="@+id/marquee_btn"
        android:text="marqueeBtn"
        android:layout_gravity="center_horizontal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        />

    <View
        android:background="#000000"
        android:layout_width="fill_parent"
        android:layout_height="1px"/>

    <widgets.GradualTextView
        android:id="@+id/gradual_tv"
        android:text="100"
        android:textSize="100sp"
        android:layout_gravity="center_horizontal"
        android:layout_width="wrap_content"
        shinx:topColor = "#00ff00"
        shinx:bottomColor = "#ff0000"
        android:layout_height="wrap_content" />

</LinearLayout>
